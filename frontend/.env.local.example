# Backend runtime URL (optional)
# Leave blank in Cloudflare so the static frontend calls the same Worker origin.
NEXT_PUBLIC_RUNTIME_URL=http://localhost:8080

# Supabase public client config (required for waitlist + magic-link auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
