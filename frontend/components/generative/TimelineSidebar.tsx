"use client";

/**
 * TimelineSidebar — kept as a minimal re-export for backward compatibility.
 * The route timeline is now rendered directly inside RoutePlannerWizard.
 */

export { default as RouteTimeline } from "./RouteTimeline";
