# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.venv/
venv/
ENV/
env/
node_modules/

# uv
.python-version

# pytest
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.cover
.hypothesis/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Project specific
output/
outputs/
# Raw source files documented in docs/data-sources.md
data/raw/
*.log
*.pdf
*.html
!templates/*.html
!supabase/templates/*.html
!docs/mockups/**/*.html
!docs/design/**/*.html

# QA/debug screenshots dumped at repo root (root-level only; docs/ assets unaffected)
/*.png

# Test outputs
test_results.json
e2e/.auth/

# Git worktrees
.worktrees/

# Secrets
.env
.env.local
.env.test
.env.production
secrets/
credentials/
anitabi/

# AI/IDE agent configs (local only)
.agents/
.augment/
.claude/
.kiro/
.playwright-mcp/
.roo/
.superpowers/
.trae/
.mcp.json
skills/
skills-lock.json
.remember/tmp/

# Misc
images/
!apps/web/public/images/
*.pen
.next/
.open-next/
.wrangler/
.venv
node_modules
.gstack/

# Sandbox artifacts
.pg-wheels/
.sandbox-env/
.sandbox-venv/
.remember/

# Agent / tool scratch dirs (not source)
.codex/
.baoyu-skills/
.impeccable/

# Large design-probe / mockup image artifacts (kept on disk, not in git)
docs/design/redesign-probes/
docs/mockups/*
docs/mockups/previous-design-contact-sheet.jpg
!docs/mockups/review-boards/
!docs/mockups/review-boards/**
!docs/mockups/landing-hero/
!docs/mockups/landing-hero/**
backend/tests/eval/results/agent_openai-mimo-*
*.tsbuildinfo

# Catalog TS service source — re-include (root lib/ rule is too broad)
!catalog/src/lib/
!workers/catalog/src/lib/
!workers/catalog/src/lib/**

# Users TS service source — re-include (root lib/ rule is too broad)
!workers/users/src/lib/
!workers/users/src/lib/**

# apps/web TS source — re-include (root lib/ rule is too broad)
!apps/web/src/lib/
!apps/web/src/lib/**

# Pinned Atlas CLI self-heal cache (#730) — downloaded, checksum-verified
# binary for `make test-integration`; never checked in, never the global install.
/.cache/atlas/

# tool-generated local state (rebuildable)
.codegraph/
test-results/

# CI-generated build metadata (#494) — written into the checkout just before
# `wrangler deploy` builds the root container; never committed (a committed
# copy would go stale with the deploy).
/apps/agent/src/animichi/build_info.py
