{
  "name": "infra",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "node --test topology-*.test.ts && pnpm run test:program-load",
    "test:program-load": "bash ../scripts/local-gates/infra-check.sh"
  },
  "dependencies": {
    "@pulumi/cloudflare": "^6.20.0",
    "@pulumi/pulumi": "^3.262.0",
    "@pulumi/random": "^4.21.1",
    "js-yaml": "5.4.2"
  },
  "devDependencies": {
    "@types/js-yaml": "4.0.9",
    "@types/node": "catalog:",
    "smol-toml": "catalog:",
    "typescript": "catalog:"
  }
}
