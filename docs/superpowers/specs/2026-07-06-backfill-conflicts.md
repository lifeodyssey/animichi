# 回填冲突清算清单(双评审前 · 待用户裁决)

> 来源:四个回填代理(A 主spec / B iter-0,1 / C iter-2,4,5 / D iter-3,6,7)上报的、它们不敢擅自裁决的跨文件冲突。
> 主会话已核实 spec 现状。每条:问题 → 核实结论 → 推荐 → 状态。
> 裁决后回填对应 spec 并进双评审。

## C1【真缺口】routes 历史数据迁移 story 缺失

- **代理**:C + D 均撞到。
- **SD-3④ 原文**:既有「会话/消息/**routes**」三类数据迁 Neon,迭代 2-3 独立 story。
- **核实**:iter-2 S2.9 = sessions 迁移 ✓;iter-3 S3.9 = conversation_messages 迁移 ✓;iter-2 **S2.8 是在 Neon 新建 routes 表**(用户域新表),**但既有 Supabase routes 数据的迁移 story 确实没有**。三缺一属实。
- **推荐**:轻量补。因 SD-3④ 明言「prod 数据量近零」,且 S2.8 已在 Neon 建 routes 表——**方案 a(推荐)**:在 S2.9 迁移脚本范围里加一句「顺带迁 routes 表既有行(近零)」,不单开 story;**方案 b**:S2.8 补一条 AC「若 Supabase 有既有 routes 行则一次性搬入」。二者都不新增 story。
- **状态**:待用户选 a/b。

## C2【架构级 · 重点】迭代7 MCP server 落 Python 还是 Workers

- **代理**:D(自评为本次最大修正)。
- **冲突**:SD-25② 原文写「FastMCP.from_openapi(openapi.json)」——FastMCP 是 **Python 库**;但项目默认认知是 **Workers/TS 优先**(catalog、users 域都在 Workers)。D 据原文把 MCP server 落到了 `apps/agent`(Python 侧)。
- **核实**:SD-4 已定 Python 容器不再议;SD-25 的单一真源是 /v1 OpenAPI。MCP server 只是读 OpenAPI 生成的薄壳,落哪端都能调同一 /v1。
- **权衡**:① 落 Python(FastMCP.from_openapi 一行白嫖,但多一个 Python 部署面)② 落 Workers/TS(与既有网关同栈,但要找 TS 的 OpenAPI→MCP 生成器,未必一行)③ 迭代7 才做,现在只需定方向不需定实现。
- **推荐**:**接受 D 的处理(Python/FastMCP),但标注为「迭代7 开工前复核」**——因为 MCP 2026-07-28 无状态核心 + FastMCP 生态成熟度届时可能变,且这是 future 迭代,锁死过早。**这条我建议也丢给双评审重点看。**
- **状态**:待用户认可"暂定 Python + 迭代7 复核",或明确要求 Workers。

## C3【判断已合理】SEO 内容迭代归属:迭代2 → 迭代5

- **代理**:C 发现并已处理(内容落 iter-5,iter-2 只标注)。
- **冲突**:seo-geo-plan §7 迭代映射把「作品页 JSON-LD + 事实速览块 + hreflang」标在「迭代2 详情+列表」行。
- **核实**:iter-2 实际交付 `/routes/:id` + `/routes`——**都是用户私有页**(不 SSR/不进 sitemap/无 hreflang 意义);`/anime/:id` 作品页要**迭代5**才建,SD-27 也明言 programmatic SEO 主战场在迭代5。**映射表那一行是笔误**,C 判断正确。
- **推荐**:**采纳 C 的处理**(SEO 内容归迭代5),并**修 seo-geo-plan §7 映射表**把那行从「迭代2」改到「迭代5」消除笔误源头。
- **状态**:待用户点头(纯确认)。

## C4【澄清即可】匿名认领:S1.7(会话/记忆)vs S2.8(路线/收藏)

- **代理**:B。
- **冲突**:SD-15② 把「匿名→登录**会话归属**迁移」放迭代1(B 已落 S1.7);S2.8 另有「匿名**路线保存**认领」(迭代2)。B 怕两者重叠遗漏。
- **核实**:两者**数据层不同**——S1.7 迁的是会话事实台账+消息历史(设备token→user_id);S2.8 认领的是 routes 表的 user_id。**不冲突,是互补的两半**。
- **推荐**:**无需改动**,仅在两处各留一句交叉引用(B 已加)。确认边界清晰即可。
- **状态**:待用户点头(纯确认)。

## C5【工程分解 · 已合理】动态OG 跨迭代拆分 + 图搜story落点

- **代理**:C。
- **两个子项**:① 动态OG:通用渲染管线+分享页模板落迭代4,作品页模板落迭代5(因作品页迭代5才存在)——合理工程分解;② 图搜阶段2(S4.8/S4.9)落点路径是后端 `apps/agent/infrastructure/vision_search/`,而 iter-4 主题是前端「しおり」,C 问是否该拆独立 backend 迭代。
- **推荐**:**①采纳**(跨迭代管线复用是对的);**②保持现状**(图搜阶段2 和迭代4 対比図共享参考图管线,同迭代内前后端 story 混编是本项目既有形态=全栈纵切 G4,不必为"纯度"另拆迭代)。
- **状态**:待用户点头(纯确认)。

## C6【主spec 遗留 [提案待确认] · A 上报】四处"SD 未显式再确认的子条款"

A 严守"不在列表里就不擅自定案",留了四处 [提案待确认],需裁决是否升定案或维持:
- **C6a genUI payload URL 白名单**(仅 catalog/白名单来源渲染):SD-13 confirmed 总哲学,但未显式再确认这条 URL 子条款。**推荐升定案**——它是 SD-19 注入防御「payload 内 URL 仅白名单来源」的同源要求,实为已覆盖,A 只是没权跨条引用。
- **C6b D18**(BYOK 只覆盖主循环,内部调用用服务端 key):SD-20 一致但未单独重申。**推荐升定案**——SD-20 的 D18 边界回归 AC 已隐含,补一句显式化。
- **C6c D16/D19**(换模型过 eval gate / 不做模型分层):§十/十一 无对应轮次。**推荐维持现状定案**——这两条是更早已认可的默认项(见补章 II 模型策略),非新开,A 因不在列表故未动;确认它们仍有效即可。
- **C6d SD-12 与 A2A 并提措辞**:SD-12 定义能力形态(任务型)时旧文并提"MCP/A2A",现 A2A 已押后(SD-25③)读来别扭。**推荐软化措辞**:SD-12 能力形态定义保留,删去与 A2A 的并提,改为"经 MCP 暴露;A2A 见 DD-10"。
- **状态**:C6a/b 升定案、C6c 确认有效、C6d 软化措辞——均为文字层,待用户一句"照推荐"即可批量处理。

## 汇总:需你实质决策的只有 2 条,其余纯确认

- **C1**:routes 迁移补法选 a(并入S2.9)还是 b(S2.8 加AC)—— 推荐 a
- **C2**:MCP server 暂定 Python+迭代7复核 认不认 —— 推荐认,且丢双评审
- C3/C4/C5/C6:代理判断合理/文字层处理,推荐"照推荐"批量过,无实质分歧
