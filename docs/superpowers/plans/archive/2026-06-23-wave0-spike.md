# Wave 0 — Spike: 验证关键假设 (Platform Monorepo + CF Deploy)

> **For agentic workers:** REQUIRED SUB-SKILL: 这是 **spike**(验证导向,非 TDD)。用 superpowers:executing-plans 内联执行(spike 需凭据交互 + 现场判断,不宜全 subagent)。步骤用 checkbox(`- [ ]`)跟踪。每个 Task = 一个假设,有明确 pass/fail 判据 + 失败回退。

**Goal:** 在不碰 prod 的前提下,实测验证 spec 的 4 个关键假设(OpenNext 独立 Worker build、route-based 多 Worker 分流、Pulumi+R2 state backend、agent 容器 build+push),绿了才进 Wave 1;失败则记录并触发 spec §7 的回退方案。

**Architecture:** 用 `*.workers.dev` 临时 Worker + 临时 route + 临时 R2 bucket 做验证,prod(v0.2.0)与其 route 全程不碰。spike 产出 = 一份"假设验证结果"(各 ✓/✗ + 证据 + 对 Wave 1 的影响)。代码产物只保留确定要用的(`frontend/open-next.config.ts`、`infra/` Pulumi scaffold);临时云资源用完即删。

**Tech Stack:** Cloudflare Workers + Routes + Containers + R2;OpenNext(`@opennextjs/cloudflare`);Pulumi(TypeScript + `@pulumi/cloudflare`);wrangler;Node 24;Docker。

## Global Constraints

- worktree:`backend-survey`(`/Users/lumimamini/Documents/Seichijunrei-agent/.claude/worktrees/backend-survey`)。所有命令从此目录。
- Node 24:每个 node/npm/npx/wrangler 命令前 `source ~/.nvm/nvm.sh && nvm use 24`(或用 `~/.nvm/versions/node/v24.15.0/bin/`)。验证 `node -v` = v24.x。
- **prod 不可破坏**:不碰 `seichijunrei.zhenjia.org` 的现有 route、不碰 prod 的 worker `seichijunrei`/`catalog`、不对 prod Supabase 跑迁移。spike 用临时名(后缀 `-spike`)+ `*.workers.dev` + 临时 R2 bucket。
- 凭据(spike 前置,见 Task 0):`CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`(main 的 `.env`,prod valid,account `021233c1880a43aa68565496100e1f8c`);**R2 S3 token + Pulumi passphrase 需用户提供**。
- 失败处理:任一假设 ✗ → 在 Task 5 记录证据 + 触发回退(route→service-binding;Pulumi→wrangler.toml route),**不**强行继续 Wave 1。
- 清理:每个 Task 末尾删除它建的临时云资源(临时 worker / route / R2 测试对象)。
- 不抑制、不碰前端 React/Next 业务代码。

---

### Task 0: 环境与凭据就绪(spike 前置)

**Files:**
- 读: `~/Documents/Seichijunrei-agent/.env`(main worktree,gitignored,prod 凭据)

**Interfaces:**
- Produces: 环境变量 `CLOUDFLARE_API_TOKEN`、`CLOUDFLARE_ACCOUNT_ID`、`R2_ACCESS_KEY_ID`、`R2_SECRET_ACCESS_KEY`、`PULUMI_CONFIG_PASSPHRASE`(供 Task 2/3/4)。

- [ ] **Step 1: 确认 Node 24 + Docker + wrangler**

```bash
source ~/.nvm/nvm.sh && nvm use 24 && node -v   # 期望 v24.x
docker info >/dev/null 2>&1 && echo "docker OK" || echo "docker NOT running"   # Task 4 需要
npx wrangler --version                          # 期望 wrangler 4.x
```
Pass:node v24.x + docker OK + wrangler 4.x。失败:docker 没起 → 提示用户启动 Docker Desktop(Task 4 才需,可先做 1-3)。

- [ ] **Step 2: 载入 Cloudflare 凭据(prod token,只读/部署临时资源)**

```bash
set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
echo "CF token len: ${#CLOUDFLARE_API_TOKEN}  account: ${CLOUDFLARE_ACCOUNT_ID:0:8}…"
```
Pass:token 非空 + account = `021233c1…`。

- [ ] **Step 3: 取得 R2 + Pulumi 凭据(需用户提供)**

R2 S3-compatible token(用于 Pulumi state backend)+ Pulumi passphrase。**若 .env 没有,停下问用户**:
> "Task 3 需要:① 一个 R2 S3 API token(R2 → Manage R2 API Tokens → 建 token,给 Object Read & Write),设成 `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`;② 一个 Pulumi passphrase(任意强口令,设 `PULUMI_CONFIG_PASSPHRASE`)。可用 `! export R2_ACCESS_KEY_ID=… R2_SECRET_ACCESS_KEY=… PULUMI_CONFIG_PASSPHRASE=…` 在本会话设。"

```bash
echo "R2 key: ${R2_ACCESS_KEY_ID:+set}  passphrase: ${PULUMI_CONFIG_PASSPHRASE:+set}"
```
Pass:两者 = `set`。失败:Task 3 阻塞,先做 Task 1/2/4。

---

### Task 1: 假设① — OpenNext 能 build 出独立 Worker 产物

**Files:**
- Create: `frontend/open-next.config.ts`

**Interfaces:**
- Produces: `frontend/.open-next/worker.js` + `frontend/.open-next/assets/`(供 Wave 1/3 web Worker)。`open-next.config.ts`(保留到 Wave 1)。

- [ ] **Step 1: 加 open-next.config.ts**

Create `frontend/open-next.config.ts`:
```ts
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
```

- [ ] **Step 2: 跑 OpenNext build**

```bash
source ~/.nvm/nvm.sh && nvm use 24
cd frontend && npx opennextjs-cloudflare build 2>&1 | tail -30
```
Expected:build 走完,末尾无 "No `open-next.config.ts`" 错误。

- [ ] **Step 3: 确认产物 + 位置**

```bash
ls -la frontend/.open-next/worker.js frontend/.open-next/assets 2>&1 | head
```
**Pass:** `frontend/.open-next/worker.js` 存在 + `frontend/.open-next/assets/` 非空。
**Fail 回退:** 记录 build error;若是 Node/依赖问题 → 调 `@opennextjs/cloudflare` 版本或 next.config;若 OpenNext 根本不产 worker.js → 假设① ✗,Wave 1 需另找前端→Worker 方案(影响整个 ii)。

- [ ] **Step 4: 记录产物路径(供 Wave 1)**

把"产物在 `frontend/.open-next/`(将来 `apps/web/.open-next/`),worker.js + assets 同目录"记进 Task 5 结果。

- [ ] **Step 5: Commit 配置(产物 gitignored,不提交)**

```bash
cd ~/Documents/Seichijunrei-agent/.claude/worktrees/backend-survey
git add frontend/open-next.config.ts
git commit -m "spike(wave0): add open-next.config.ts (required for OpenNext Cloudflare build)"
```

---

### Task 2: 假设② — route-based 多 Worker 分流(最具体优先)

**Files:**
- 临时: `/tmp/spike-edge/`(临时 edge worker)、`/tmp/spike-web-wrangler.jsonc`

**Interfaces:**
- Consumes: Task 1 的 `frontend/.open-next/`。
- Produces: 验证结论"`domain/v1/*` 比 `domain/*` 优先匹配"(供 Wave 3 拓扑)。

- [ ] **Step 1: 部署 web 临时 Worker(OpenNext 产物)到 workers.dev**

临时 wrangler 配置 `/tmp/spike-web-wrangler.jsonc`:
```jsonc
{
  "name": "seichijunrei-web-spike",
  "main": ".open-next/worker.js",
  "compatibility_date": "2026-06-05",
  "compatibility_flags": ["nodejs_compat"],
  "assets": { "directory": ".open-next/assets", "binding": "ASSETS" },
  "workers_dev": true
}
```
```bash
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
cd frontend && npx wrangler deploy --config /tmp/spike-web-wrangler.jsonc 2>&1 | tail -8
```
Expected:部署成功,打印 `seichijunrei-web-spike.<subdomain>.workers.dev`。
Pass:`curl -s https://seichijunrei-web-spike.<subdomain>.workers.dev/ | head` 返回前端 HTML。

- [ ] **Step 2: 在 prod zone 加两条临时 route 指向两个 spike worker**

用一个**不影响 prod 的临时子路径**验证优先级(不碰 `seichijunrei.zhenjia.org/*` 现有 route)。建临时 edge spike worker `/tmp/spike-edge/`:
```bash
mkdir -p /tmp/spike-edge
cat > /tmp/spike-edge/index.js <<'EOF'
export default { fetch(req) { return new Response("EDGE:" + new URL(req.url).pathname); } };
EOF
cat > /tmp/spike-edge/wrangler.jsonc <<'EOF'
{ "name": "seichijunrei-edge-spike", "main": "index.js", "compatibility_date": "2026-06-05", "workers_dev": true }
EOF
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
cd /tmp/spike-edge && npx wrangler deploy 2>&1 | tail -5
```
然后用 `wrangler` 加临时 routes(用一个 spike 子域,**不是** prod 的 seichijunrei):若无空闲子域,改用 `wrangler dev` 本地多-worker 或直接信 CF 文档的"most-specific wins"。记录采用哪种验证方式。

- [ ] **Step 3: 验证最具体优先(实测或文档佐证)**

实测路径:对配了 `spike.zhenjia.org/*`→web、`spike.zhenjia.org/v1/*`→edge 的临时子域:
```bash
curl -s https://spike.zhenjia.org/        | head -c 60   # 期望 web HTML
curl -s https://spike.zhenjia.org/v1/ping | head -c 60   # 期望 "EDGE:/v1/ping"
```
**Pass:** `/` 命中 web、`/v1/*` 命中 edge(最具体优先成立)。
**Fail 回退:** 若 CF 不按最具体优先(或无法用临时子域实测)→ 记录;假设② 部分 ✗ → Wave 3 改用 ii 的 service-binding 转发(spec §0 备选)。

- [ ] **Step 4: 清理临时资源**

```bash
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
npx wrangler delete --name seichijunrei-web-spike 2>&1 | tail -2
npx wrangler delete --name seichijunrei-edge-spike 2>&1 | tail -2
# 删除临时 routes(若建了):wrangler 或 dashboard
rm -rf /tmp/spike-edge /tmp/spike-web-wrangler.jsonc
```
Pass:两个 spike worker + 临时 route 已删,prod route 未动(`curl -sI https://seichijunrei.zhenjia.org/` 仍 200)。

---

### Task 3: 假设③ — Pulumi + R2 state backend + Pulumi×wrangler route 边界

**Files:**
- Create(spike,保留): `infra/Pulumi.yaml`、`infra/package.json`、`infra/index.ts`、`infra/.gitignore`

**Interfaces:**
- Consumes: Task 0 的 `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`PULUMI_CONFIG_PASSPHRASE`/`CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`。
- Produces: 验证结论"R2 可作 Pulumi state backend"+"wrangler deploy 不清 Pulumi-managed route"(供 Wave 2-4 IaC 边界)。

- [ ] **Step 1: 装 Pulumi CLI**

```bash
curl -fsSL https://get.pulumi.com | sh && export PATH=$HOME/.pulumi/bin:$PATH && pulumi version
```
Pass:打印 pulumi 版本(v3.x)。

- [ ] **Step 2: 建临时 R2 state bucket + scaffold infra/**

```bash
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
npx wrangler r2 bucket create seichijunrei-pulumi-state-spike 2>&1 | tail -3
```
Create `infra/package.json`:
```json
{ "name": "infra", "private": true, "dependencies": { "@pulumi/pulumi": "^3.0.0", "@pulumi/cloudflare": "^6.0.0" } }
```
Create `infra/Pulumi.yaml`:
```yaml
name: seichijunrei-infra
runtime: nodejs
description: Cloudflare infra (routes, hyperdrive, r2, dns, secrets)
```
Create `infra/index.ts`:
```ts
import * as cloudflare from "@pulumi/cloudflare";

// SPIKE: one throwaway workers route to prove Pulumi manages CF routes.
const accountId = process.env.CLOUDFLARE_ACCOUNT_ID!;
const zoneId = process.env.SPIKE_ZONE_ID!; // zhenjia.org zone id

export const spikeRoute = new cloudflare.WorkersRoute("spike-route", {
  zoneId,
  pattern: "spike-pulumi.zhenjia.org/*",
  // intentionally no script → reserves pattern without binding a worker
});
```
Create `infra/.gitignore`:
```
node_modules/
```
```bash
cd infra && npm install 2>&1 | tail -3
```

- [ ] **Step 3: pulumi login 到 R2(S3-compatible)+ init stack**

```bash
export PATH=$HOME/.pulumi/bin:$PATH
export AWS_ACCESS_KEY_ID="$R2_ACCESS_KEY_ID" AWS_SECRET_ACCESS_KEY="$R2_SECRET_ACCESS_KEY"
export AWS_REGION=auto
pulumi login "s3://seichijunrei-pulumi-state-spike?endpoint=${CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com&s3ForcePathStyle=true&region=auto"
cd infra && pulumi stack init spike 2>&1 | tail -3
```
**Pass:** `pulumi login` 成功(打印 "Logged in to s3://…")+ stack init 成功。
**Fail 回退:** R2 S3 endpoint 格式不被 Pulumi 接受 → 试 `endpoint=https://…`/调 query 参数;仍失败 → 记录,Pulumi state 退回 Pulumi Cloud 免费 tier(spec §3 备选)。

- [ ] **Step 4: pulumi up 管 1 个测试 route**

```bash
export PATH=$HOME/.pulumi/bin:$PATH
export SPIKE_ZONE_ID="$(npx --prefix ~/Documents/Seichijunrei-agent/.claude/worktrees/backend-survey/frontend wrangler whoami >/dev/null 2>&1; echo)" # zone id 见下
# zone id 获取:dashboard 或 `curl -s -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" "https://api.cloudflare.com/client/v4/zones?name=zhenjia.org" | python3 -c 'import sys,json;print(json.load(sys.stdin)["result"][0]["id"])'`
cd infra && CLOUDFLARE_API_TOKEN="$CLOUDFLARE_API_TOKEN" pulumi up --yes 2>&1 | tail -15
```
**Pass:** `pulumi up` 成功创建 `spike-pulumi.zhenjia.org/*` route;`pulumi stack output spikeRoute` 有值;R2 bucket 里有 state 对象(`npx wrangler r2 object get seichijunrei-pulumi-state-spike/.pulumi/...` 或 dashboard 确认)。
**Fail 回退:** 记录;`@pulumi/cloudflare` v6 的 WorkersRoute 资源名/字段若变 → 查 provider 文档修正。

- [ ] **Step 5: 验证 wrangler deploy 不清 Pulumi-managed route(关键边界)**

部署一个**无 route 声明**的临时 worker,确认它不影响 Pulumi 建的 route:
```bash
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
mkdir -p /tmp/spike-noroute && cd /tmp/spike-noroute
echo 'export default { fetch(){ return new Response("ok"); } };' > index.js
echo '{ "name":"spike-noroute","main":"index.js","compatibility_date":"2026-06-05","workers_dev":true }' > wrangler.jsonc
npx wrangler deploy 2>&1 | tail -3
# 确认 Pulumi route 还在:
curl -s -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" "https://api.cloudflare.com/client/v4/zones/$SPIKE_ZONE_ID/workers/routes" | python3 -c 'import sys,json;[print(r["pattern"]) for r in json.load(sys.stdin)["result"]]' | grep spike-pulumi && echo "PULUMI ROUTE INTACT ✓"
```
**Pass:** `spike-pulumi.zhenjia.org/*` 仍在(wrangler 没清它)。
**Fail 回退:** 若 wrangler 清了 → 记录;Wave 中 route 改回 wrangler.toml 管(Pulumi 只管 Hyperdrive/R2/DNS/secrets,spec §7 备选)。

- [ ] **Step 6: 清理临时资源 + commit infra scaffold**

```bash
export PATH=$HOME/.pulumi/bin:$PATH
cd ~/Documents/Seichijunrei-agent/.claude/worktrees/backend-survey/infra && pulumi destroy --yes 2>&1 | tail -5
pulumi stack rm spike --yes 2>&1 | tail -2
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
npx wrangler delete --name spike-noroute 2>&1 | tail -2
npx wrangler r2 bucket delete seichijunrei-pulumi-state-spike 2>&1 | tail -2   # 若 spike 完全结束
rm -rf /tmp/spike-noroute
cd ~/Documents/Seichijunrei-agent/.claude/worktrees/backend-survey
git add infra/Pulumi.yaml infra/package.json infra/index.ts infra/.gitignore
git commit -m "spike(wave0): infra/ Pulumi scaffold (R2 state backend validated)"
```
Pass:临时 route/worker/bucket 已删;infra scaffold 已 commit(供 Wave 2 扩展)。

---

### Task 4: 假设④ — agent 容器能 build+push

**Files:**
- 临时: `/tmp/spike-container-wrangler.jsonc`

**Interfaces:**
- Consumes: 现有 `Dockerfile`(repo root,打包 agent)。
- Produces: 验证结论"wrangler deploy 能 Docker build+push agent 容器镜像"(供 Wave 3 edge+容器)。

- [ ] **Step 1: 确认 Docker 在跑**

```bash
docker info >/dev/null 2>&1 && echo "docker OK" || echo "START Docker Desktop first"
```
Pass:docker OK。失败:提示用户启动 Docker。

- [ ] **Step 2: 临时 worker + 容器配置(指向现有 Dockerfile)**

```bash
cat > /tmp/spike-container-wrangler.jsonc <<'EOF'
{
  "name": "seichijunrei-container-spike",
  "main": "worker/entry-spike.js",
  "compatibility_date": "2026-06-05",
  "workers_dev": true,
  "containers": [{ "class_name": "RuntimeContainerSpike", "image": "./Dockerfile", "max_instances": 1 }],
  "durable_objects": { "bindings": [{ "name": "CONTAINER", "class_name": "RuntimeContainerSpike" }] },
  "migrations": [{ "tag": "v1", "new_sqlite_classes": ["RuntimeContainerSpike"] }]
}
EOF
cat > worker/entry-spike.js <<'EOF'
import { Container } from "@cloudflare/containers";
export class RuntimeContainerSpike extends Container { defaultPort = 8080; sleepAfter = "2m"; }
export default { fetch() { return new Response("container spike ok"); } };
EOF
```

- [ ] **Step 3: wrangler deploy(触发 Docker build+push)**

```bash
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
npx wrangler deploy --config /tmp/spike-container-wrangler.jsonc 2>&1 | tail -20
```
**Pass:** 日志出现 "Building image" + "Pushing" + 部署成功(镜像进 CF Registry)。
**Fail 回退:** 记录 error(Docker build 失败 → 看 Dockerfile;push 失败 → 看 token 权限需含 Containers/Registry)。容器是 CF 较新功能,IaC/部署细节以此实测为准。

- [ ] **Step 4: 清理**

```bash
source ~/.nvm/nvm.sh && nvm use 24; set -a; source ~/Documents/Seichijunrei-agent/.env; set +a
npx wrangler delete --name seichijunrei-container-spike 2>&1 | tail -2
rm -f worker/entry-spike.js /tmp/spike-container-wrangler.jsonc
git status --porcelain worker/entry-spike.js   # 应为空(已删)
```
Pass:临时 worker + 容器删除,无残留文件。

---

### Task 5: 汇总假设验证结果 + 对 Wave 1 的决策

**Files:**
- Create: `docs/superpowers/specs/2026-06-23-wave0-spike-results.md`

- [ ] **Step 1: 写验证结果**

按下表填(每项 ✓/✗ + 证据 + 对 Wave 1 影响 + 触发的回退):

```markdown
# Wave 0 Spike 结果 (2026-06-23)
| 假设 | 结果 | 证据 | Wave 1 影响 / 回退 |
|---|---|---|---|
| ① OpenNext 独立 Worker build | ✓/✗ | .open-next/ 产物路径 / build error | … |
| ② route-based 最具体优先 | ✓/✗ | curl 分流结果 / 文档佐证 | ✗→改 service-binding |
| ③ Pulumi + R2 state backend | ✓/✗ | pulumi login/up 输出 | ✗→Pulumi Cloud |
| ③b wrangler 不清 Pulumi route | ✓/✗ | route 仍在的 API 输出 | ✗→route 归 wrangler.toml |
| ④ agent 容器 build+push | ✓/✗ | wrangler build/push 日志 | … |
绿灯门:①②③④ 全 ✓ → 进 Wave 1。任一 ✗ → 按回退调整 spec 再进。
```

- [ ] **Step 2: Commit 结果**

```bash
git add docs/superpowers/specs/2026-06-23-wave0-spike-results.md
git commit -m "spike(wave0): hypothesis validation results"
```

- [ ] **Step 3: 决策门**

全 ✓ → 报告用户"Wave 0 全绿,进 Wave 1(monorepo 骨架)"。有 ✗ → 报告用户哪条失败 + 回退建议,等确认再调 spec。

---

## Self-Review

- **Spec coverage:** spec §5 Wave 0 的 4 项假设 → Task 1/2/3/4 一一对应;Pulumi×wrangler 边界(spec §3/§7)→ Task 3 Step 5;R2 state backend(spec §3)→ Task 3。✓
- **Placeholder scan:** 无 TBD;凭据/zone-id 用明确获取命令;临时资源名具体。spike 的"实测或文档佐证"是 spike 性质(有判据),非占位。
- **Consistency:** worker 名 `seichijunrei-*-spike` 全程一致;R2 bucket `seichijunrei-pulumi-state-spike` 一致;Pulumi 资源 `spike-route`/`spikeRoute` 一致。
- 注:Task 3 Step 4 的 SPIKE_ZONE_ID 获取命令以注释里的 `curl …zones?name=zhenjia.org` 为准(执行时先取 zone id 再 export)。

## Execution Handoff
推荐 **Inline Execution**(superpowers:executing-plans):spike 需凭据交互(Task 0 Step 3 可能要停下问用户)+ 现场判断 pass/fail + 云资源清理,不宜全 subagent 自动跑。
