# Orca coordinator handoff — 2026-09-21

Live snapshot at 2026-09-21T13:45Z. This file is the pickup point;
[HANDOFF-2026-09-20.md](HANDOFF-2026-09-20.md) and [HANDOFF.md](HANDOFF.md) are earlier and
stale. Companion lessons: [LESSONS.md](LESSONS.md).

**Role:** Orca coordinator. Dispatch writers through the headless launcher; never write
product code. Own commits, PRs, merges, and the board. Ask the owner only about money,
production, secrets, scope, or rule exemptions.

## Read this before you trust the rest of this file

The 2026-09-20 handoff said #1825 and #1832 needed "Owner round-4 or human. Do not merge."
Both were wrong. The round-3 reports it cited — which it linked — say
`0 must-fix on both axes` and `Both axes PASS`. The board sat still for a day because the
previous coordinator's **inference** was written down in the same voice as a **fact**, and
the next reader took it as one.

So: treat every judgement in this file as an inference. Re-verify PR state against GitHub
and review verdicts against the round reports in `/private/tmp` before acting on any
"blocked" or "needs owner" claim, including mine.

## Bindings

| | |
|---|---|
| Run | `run_0950f6f54b72` |
| Coordinator | `term_453fd488-e290-403d-ac13-19662a805045` |
| Do not ship from | `/Users/lumimamini/Documents/Seichijunrei-agent` on `fix/web-search-map-attribution` (**PR #1819**) |
| Worktrees | `/Users/lumimamini/orca/workspaces/Seichijunrei-agent/orca-<n>-lane` |
| Launcher | `orca-pi-headless-support/scripts/orca-headless/orca-headless.rb` |
| Runtime client | `/Applications/Orca.app/Contents/Resources/app.asar.unpacked/out/cli/runtime/client.js` |
| Writers | pi `bigmodel/glm-5.3-flash` max · pi `opencode-go/deepseek-v4.1-flash` max |
| Review | claude `claude-opus-5` high |
| `origin/main` | `ea811590b` |

**Gone:** grok (allowance exhausted 2026-09-21, confirmed by the owner) and command-code
(subscription expired 2026-09-20). Do not dispatch either; do not silently substitute.

Heartbeat `/private/tmp/animichi-coordinator-alive` — touch it every tick you work.
Automation `2df8074b-19b6-43a9-bb17-793fd07ef7d3` `*/30` Asia/Taipei, `agentId: claude`,
precheck `/Users/lumimamini/orca/animichi-coordinator-precheck.sh` (v5).
**Do not spawn a second coordinator.**

## Infrastructure repaired this session (do not undo)

1. **Launcher version pin** `1.4.200` → `1.4.206` in `runtime-client.cjs` and
   `orca_headless/response_verifier.rb`. Orca had updated and every dispatch hard-failed
   `unsupported_orca_version`. Evidence for the bump: the launcher's own
   `REQUIRED_CAPABILITIES` (`terminal.create-idempotency.v2`, `orchestration.contract.v1`)
   are both still advertised by 1.4.206, and the installed `out/package.json` and live
   `appVersion` both read 1.4.206. Launcher suite 96 runs green after.
   **Keep the equality check.** It is the safety valve on an unsupported internal seam; a
   range comparison would remove the thing that stops the next silent breakage.
2. **oh-my-zsh update mode** `prompt` → `reminder` in `~/.zshrc` (backup
   `.zshrc.bak-20260921`). The interactive `[Y/n]` prompt was consuming the first character
   of the injected command — `exec …` arrived as `xec …`, `command not found`. Every
   dispatch into a fresh terminal died at `prompt_published` with an empty stderr.
3. **Precheck v5.** Removed `pgrep -x grok`. Alive-file window 180s → 2400s.
4. **Automation** `agentId` grok → claude; prompt rewritten for the current writer pool,
   reviewer, launcher and handoff pointer.

## In flight at this snapshot

Six writer lanes plus one review seat, all `agent_started`:

| Lane | Card | Work | Model | State dir |
|---|---|---|---|---|
| #1818 | #469 | CodeQL `Bad HTML filtering regexp` ×2 on `apps/web/tests/csp-evaluator.ts` | GLM | `animichi-lane-1818codeql` |
| #1831 | #1632 | `codecov/patch` 84.94% vs 95% target | DS | `animichi-lane-1831codecov` |
| #1832 | #1630 | 4 CodeRabbit threads (2 Major) | GLM | `animichi-lane-1832threads` |
| #1825 | #1824 | **review** of the bot-fix delta `7b5d6b151..92200cc94` | Opus 5 | `animichi-lane-1825review` |
| #1452 | #1452 | pin `packages/contract`'s oxlint scope | DS | `animichi-lane-1452lint` |
| #1524 | #1524 | prove/close production `workers.dev` | GLM | `animichi-lane-1524workersdev` |

## Done this session

- **#1831 rebased** onto `origin/main` and force-pushed; `DIRTY` cleared, remote and local
  both `c198814b4`. The one conflict was `pnpm-workspace.yaml`'s hold-back register: main
  had retired `@pydantic/logfire-node` (#1829) and renamed the Prisma entry, while the
  branch legitimately adds `@prisma/orm-family-sql` (`declaredIn: workers/users/package.json`).
  The oracle was `dependency-hold-backs.json` — its automerged 8 entries are what the yaml
  must mirror. Resolve that file by the generated fixture, never by reading the comments.
- **#1825 bot fixes landed** as `92200cc94`: RESP reply headers bounded at 512 bytes with
  the argument spread replaced; the TCP dial held to `STORE_TIMEOUT_MS`; byte framing moved
  to a new `apps/anitabi-egress/src/resp-replies.ts` (store back to 257 lines); three
  judgement threads fixed, one declined with the residual stated. Awaiting the Opus review.

## Open PRs

| PR | Issue | Head | State | Next |
|---|---|---|---|---|
| [#1825](https://github.com/lifeodyssey/animichi/pull/1825) | #1824 | `92200cc94` (unpushed) | round 3 PASSED; 6 threads answered locally | Opus review → push → reply inline on all six → 10-min bot window → merge |
| [#1828](https://github.com/lifeodyssey/animichi/pull/1828) | #1809 | `ba97d2e42` | CLEAN, based on `orca-1824-lane` | After #1825 merges: retarget to `main`, rebase, own bot window |
| [#1832](https://github.com/lifeodyssey/animichi/pull/1832) | #1630 | `e7f1d5cb2` | round 3 PASSED; BLOCKED on 4 threads | writer running |
| [#1831](https://github.com/lifeodyssey/animichi/pull/1831) | #1632 | `c198814b4` | rebased; `codecov/patch` FAILURE | writer running |
| [#1818](https://github.com/lifeodyssey/animichi/pull/1818) | #469 | `32083575c` | CodeQL FAILURE, 2 GHAS threads | writer running |
| [#1819](https://github.com/lifeodyssey/animichi/pull/1819) | owner | | BEHIND | **Do not touch** |
| [#1817](https://github.com/lifeodyssey/animichi/pull/1817) | this docs tree | | docs only, no threads | mergeable any time |

Reports: `/private/tmp/animichi-lane-1824review3/review-round-3.md`,
`/private/tmp/animichi-lane-1630review3/review-round-3.md`,
`/private/tmp/animichi-lane-1818r4review/review-round-4.md`,
`/private/tmp/animichi-lane-1825threads/report.md`.

## Owner-only

1. **Stale grok process `pid 20705`** (2d05h, `STAT S+`). It is what disarmed the automation.
   Harmless now that precheck v5 ignores it; kill or keep is the owner's call.
2. **21 stale runner wrappers** from the 18–20 September campaign, holding PTYs and leaking
   zombies (58 at this snapshot). `ps -eo pid,args | grep runner.rb`. Their lanes:
   `orca-1792` ×8, `orca-1630` ×3, `orca-1690` ×2, `orca-1650` ×2, `orca-signkey` ×2,
   `orca-1762`, `orca-1797`, `orca-1629`.
3. **Stale cards to close**, each with an `origin/main` citation from the triage sweep:
   #1081, #1057 (absorbed by #1367/#1370); #432, #1349, #1350, #1407 (`apps/agent` deleted
   by #1756); #1399 (Python emitter gone); #1417 (`eval:staging` retired); #1557 code portion
   (landed as `aa5764766`). Needing an owner call on whether they are still live: #1001,
   the #1183/#1184/#1181/#1175 chain, and #1560's #1515 blocker.
4. After #1825 merges: `fly redis create` (primary **nrt**) then
   `fly secrets set CEILING_STORE_URL='redis://…'` on `animichi-anitabi-egress`.
   **TCP Private URL only.** Fly Redis has no REST. Do not set `CEILING_STORE_TOKEN`.
   No local Fly deploy.
5. **#1524 may be a live exposure.** Production's `workers.dev` host has no explicit
   `workers_dev = false` anywhere, and wrangler defaults it to `routes.length === 0` — the
   production blocks declare no routes. The lane measures it first; read that measurement
   before deciding anything.
6. #1819.

## Do not re-litigate

- Ceiling is TCP RESP to the Fly Redis Private URL. No REST, no `CEILING_STORE_TOKEN`.
- #1630 `import/` staying on Drizzle is an accepted cut.
- Catalog overlap → stacked PRs; #1629 could not be pushed alone.
- Bot comments and CI repairs are **fix-only** and do not consume a Matt review round. That
  is what unblocked #1825 and #1832; do not re-derive it as an owner question.

Parked: **#1771** (restack after #1832); **#1633** (after #1831); **#1627** (edits the same
`contract.prisma` that #1832 and #1831 generate clients from — fire it only after both land).

## Owner bars (do not mix)

- **Default (2026-09-15):** must-fix = written-rule violations and real defects/AC gaps.
  A squeeze that removes code is must-fix. Do not require added files/tests/abstractions.
- **#1818 round 4 only:** no security/major → push; otherwise fix then push. **No other card.**

Squash-only merge. 10-minute bot window after the **final** head. Never mix
`gh pr update-branch` with a local push on the same branch. Quota-exhausted bot notices are
non-actionable. No local deploy. One writer per candidate. `Closes #N` in the PR body.
No AI trailers — `commitlint`'s `ai-attribution-forbidden` rejects them.

## First actions

1. Bind the Run, touch the heartbeat, drain the inbox.
2. `orca orchestration worker-read --dispatch <id> --source auto` is the fleet verdict on a
   worker. Do not substitute a `ps | grep` of your own — pi's process is named `pi` and a
   hand-rolled pattern will report a working lane as dead.
3. Settle each lane: verify the tree yourself, commit, dispatch a different-model review,
   then push and answer threads inline.
4. Do not merge #1828 before #1825. Do not `fly redis create`. Do not touch #1819.
