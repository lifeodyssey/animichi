# Orca coordinator handoff — 2026-09-21, 23:30 tick

Live snapshot at 2026-09-21T15:45Z. This file is the pickup point;
[HANDOFF-2026-09-21.md](HANDOFF-2026-09-21.md) (13:45Z) and everything before it are earlier.

Same warning the 13:45Z file opens with, and it applies to this file too: **everything below
that is not a quoted command output is an inference.** Re-verify PR state against GitHub and
review verdicts against the reports in `/private/tmp` before acting on any "held" claim here.

## Bindings

Unchanged from the 13:45Z file except the two rows below.

| | |
|---|---|
| Run | `run_0950f6f54b72` (bound with `orca orchestration run-use --id …`, **not** `run-bind` — that verb does not exist) |
| Coordinator | `term_fb3646d4-5e86-4c04-9a1b-3de8b95e103f` (this tick; the 13:45Z file names the previous one) |
| `origin/main` | `ea811590b` at tick start, unchanged at tick end |

## What this tick did

### Settled and made durable (6 worker_done accepted, 6 released)

Every worker below had left its output **uncommitted in the working tree**, which is the lane
contract. This tick committed and pushed it. LOCAL/REMOTE SHA was compared after every push.

| Lane | Dispatch | Commit | Pushed |
|---|---|---|---|
| `orca-1824-lane` → PR #1825 | `ctx_f12468131ef3` | `495a12db3` `fix(catalog): check every reply in the ceiling store's set` | yes, verified |
| `orca-1630-lane` → PR #1832 | `ctx_10e725686b0d` | `3f9992949` `fix(catalog): release prisma clients once and group the cache predicate` | yes, verified |
| `orca-1632-lane` → PR #1831 | `ctx_1e5451f1b869` | `5c4c153c0` `test(users): cover the idempotency store's failure branches` | yes, verified |
| `orca-1524-lane` (no PR yet) | `ctx_848a03c4d257` | `669db8fc7` `ops(edge): pin workers.dev off on every production surface` | **not pushed — held for review, see below** |
| `orca-469-lane` → PR #1818 | `ctx_d56746856677` (review) | rebased `b5d04b69b` → `1cd9473d2` | **NO — push refused by a red gate. See below.** |
| `orca-1452-lane` (no PR yet) | `ctx_fb33819f5941` (review) | `1d7aaf285` (clean, unchanged) | push in flight at tick end |

### PR #1832 — all four CodeRabbit threads closed

Three fixed with inline replies citing `3f9992949` and the mutation proof; one
(`PRRT_…CHp5`) **declined** with the reason in the thread: `provenanceValues()` omits
`captured_at` from the insert rows, so the conflict clause is its only AST occurrence and the
existing assertion is not vacuous. All four resolved **after** the reply, never silently.

### PR #1818 — rebased, verdict preserved

`origin/lifeodyssey/orca-469-lane` was BEHIND. Rebased onto `ea811590b`. The delta hash
`git diff origin/main...HEAD | shasum -a 256` is **byte-identical before and after**
(`b83d2a41…`), so `ctx_d56746856677`'s Opus APPROVED verdict on `b5d04b69b` carries to
`1cd9473d2` without a re-review. Force-pushed with `--force-with-lease`.

**The push was refused, and PR #1818 is further behind than it looks.** The pre-push affected
gate went red: `apps/web` `tests/unit/chat/chat-page-clarify-pick.test.tsx:102`,
1 failed / 2688 passed. The gate was **not** bypassed (`--no-verify` is forbidden), so nothing
was published.

Two consequences, and the second is the one that matters:

1. `origin/lifeodyssey/orca-469-lane` is still at **`32083575c`**. That is the commit *before*
   the CSP fix. The reviewed delta was `32083575c..b5d04b69b` — so **the fix the Opus seat
   approved has never been pushed at all**, and PR #1818 today shows none of it. The rebase to
   `1cd9473d2` exists only in the local worktree.
2. `ctx_d56746856677`'s report says this exact failure is "a load flake in an untouched chat
   file that passes in isolation." That is plausible and it is **not verified here** — the
   coordinator role forbids running product tests, so no one has yet re-run that file in
   isolation on `1cd9473d2`. Do not treat "it's a flake" as established. It is the reviewer's
   claim about a *different* commit.

Next tick: dispatch a writer lane that (a) runs `chat-page-clarify-pick.test.tsx` in isolation
and under full load on `1cd9473d2`, (b) if it is a genuine load flake, fixes the flake rather
than re-running until green, and (c) only then pushes. If it is a real failure, the rebase
surfaced it and the CSP change does not ship until it is fixed.

Still open on it: two CodeQL threads on `apps/web/tests/csp-evaluator.ts`. The Opus seat
approved the fix **and** recorded that the rewritten doc comment above it is false — an end
tag does not end at its first `>` (a `>` inside a quoted attribute value does not terminate
one, and `</scriptx>` is not an end tag at all). The code fails safe; **the justification
written above it does not.** That is a fix-only writer task, not a review round. Not dispatched
this tick.

### Launched (2 review lanes, both `claude/claude-opus-5/high`)

| Lane | Dispatch | Candidate | Writer (must differ) |
|---|---|---|---|
| `/private/tmp/animichi-lane-1825r2review` | `ctx_b36c63551f2d` | PR #1825 delta `09f4d7c65..495a12db3` | `opencode-go/deepseek-v4.1-flash` |
| `/private/tmp/animichi-lane-1524review` | `ctx_83358bdf1323` | #1524 round-1, `origin/main..669db8fc7` | `bigmodel/glm-5.3-flash` |

Both briefs **forbid Docker / `packages/test-postgres` / any integration suite** — the machine's
one heavy slot belongs to the #1557 eval lane this tick. Both briefs name the specific
writer claim that is load-bearing and tell the seat to go read the primary source rather than
accept it (for #1524 that is wrangler 4.132.0's `getSubdomainValues` in `cli.js`).

### #1557 — the blocked eval lane was answered: **B**

`ctx_893cc8f0fc37` (pi, `orca-1557-lane`) had been holding on a question since 15:2xZ and was
**not** starting any paid run. It offered A (a real catalog origin / DSN), B (local fixture
catalog), C (leave AC-1 blocked).

Answered **B**, and the reason matters for the next coordinator: B is not a compromise, it is
what the card says. #1557's own Scope section reads *"the existing local catalog plus
`packages/test-postgres` data plane remains available and is not replaced with tool mocks."*
The prohibition is on **tool mocks**, not on a local catalog. AC-1 is also a **mechanism**
criterion — it records commands, model identity, commit, outcomes and spend; it asserts no
quality threshold — so a fixture-backed run satisfies it provided the provenance is stated in
the AC-1 evidence line itself. A refused outright: handing a worker a production data-plane DSN
is owner-gated (money + production) and not a scheduled-tick decision.

Two of that worker's measurements were sent back as **unverified, do not repeat as fact**:
- "both Neon branches hold only `neon_auth.*` tables" names project `billowing-fire-22850320`.
  That may be the Neon Auth project rather than the data plane; prior repo evidence records ~34
  tables on staging. It does not change the answer, because B does not touch Neon.
- `staging.animichi.com` answering 302 Cloudflare Access is an **access boundary**, not proof
  the catalog is empty.

Spend controls given: smoke 2–3 cases first and stop if not clean; paid runs only through the
`opencode-go` binding; escalate rather than continue if the 33-case projection exceeds the
smoke's implication by more than ~2×.

## Held, with the reason

| Item | Held because |
|---|---|
| **Merges on web / visible-UI PRs** (#1818) | The owner's 2026-09-21 ~21:58 local instruction reads "route frontend cards to kimi k3-256k-max, **no self-merge**, require before/after screenshots in PR comments". All three clauses sit in one frontend/visible-UI sentence, and the screenshot clause is already recorded as scoped to that subset. So the face-value reading scopes the no-self-merge clause there too. Held for those PRs; one sentence from the owner would settle whether it is broader. |
| Merges on backend PRs (#1825, #1831, #1832) | **Not held by policy** — `feedback_merge_authority` still applies: CI green + threads clear + no blocking review verdict means the coordinator merges. None were mergeable *this* tick for ordinary reasons: CI is re-running on all three after this tick's pushes, and #1825/#1831/#1832 still owe an Opus delta verdict. Resume normally next tick; do not read this row as an owner gate. |
| Reviews of PR #1831 and PR #1832 | Both need real-Postgres integration runs to reproduce the writers' mutation proofs. The heavy DB/Docker slot is the #1557 eval lane. Next tick, once #1557 settles. A review seat that cannot reproduce the mutations is not worth dispatching — mutation proof is the only green-light evidence this repo accepts. |
| #1524 PR creation | Deliberate pipeline order: round-1 review verdict first, then the PR. The brief asks the seat for an explicit "may this open a PR" verdict. |
| #1818 doc-comment fix | Real finding from the Opus seat (false claim about end-tag parsing). Fix-only, needs a writer lane, does not consume a review round. |
| **#1560** `feat(eval): recapture the baseline and wire the lanes` | Labelled `ready-for-agent`, and both GitHub blockers (#1558, #1559) are **CLOSED** — so it looks dispatchable and is not. Two real reasons: it writes `packages/eval/**`, which the live #1557 lane owns right now (one writer per candidate), and its AC-1 needs a **full paid `profile_v1` run** that presupposes #1557's in-process harness actually works. Dispatch it after #1557 lands, not before. |
| #1565, #1367 | Labelled `ready-for-human`, not agent-dispatchable. #1367 additionally blocked by #1364/#1365. |
| #1570 | Parked by the owner. |
| `orca-1629-lane` (`cde1b3557`), `orca-1771-lane` (`4e032ada7`) | Real commits on the catalog→prisma refactor, **no PR ever opened**, not merged. Plausibly superseded by the merged #1820/#1827/#1828 line, but that is a guess, not a check. Someone should diff them against main and either open a PR or delete them. |
| `ctx_9603ce7eb884`, `ctx_caa0a992e0c8`, `ctx_74ec41effbbf` | `dispatched`/`in_progress`, no workspace, liveness `unverifiable`. That is **absence, not death** — absence authorises no stop, abandon or release. Left alone. |

## Cleanup candidates (not done this tick)

These lanes' branches are gone from `origin` (squash-merged, so the tip is never an ancestor of
main — an ancestor check reports them as unmerged and is the wrong check here):
`orca-1650-lane` (#1815), `orca-1690-lane` (#1814), `orca-1762-lane` (#1808),
`orca-1792-lane` (#1806), `orca-1797-lane` (#1807), `orca-signkey-lane` (#1812).

Remove with `orca worktree rm --worktree id:<repoId>::<path>`, not raw git, and only after
confirming no retained terminal is attached.

## Two things that cost time this tick

**`timeout` does not exist on macOS.** `timeout 900 git push …` failed with
`command not found`, and the `echo "EXIT=$?"` that followed reported `EXIT=0` — because it was
reporting `echo`'s status, not the push's. The push had not happened. The only reason this was
caught is that the same command printed LOCAL and REMOTE SHAs and they differed. Keep comparing
LOCAL to REMOTE after every push; `feedback_git_commit_phantom` is still earning its keep.

**`orca orchestration run-bind` is not a command.** The verb is `run-use`, and it takes
`--id`, not `--run`.

**Rebinding the Run fences the previous consumer, and that breaks launcher cleanup.**
`orca orchestration run-use --id run_0950f6f54b72` bumped `consumer_generation` to 19. Afterwards
`orca-headless.rb cleanup --state-dir … --settlement-message msg_26d55ef8d3aa` refused with
`consumer_fenced`: it verifies settlement through that `worker_done` message, and the message
belongs to the fenced generation.

That fencing is deliberate — it stops an old receipt from authorising cleanup of a newer
terminal — but the consequence is that **lanes started under a previous tick's binding cannot be
cleaned up by the launcher after a rebind.** Their `runner.rb` wrappers keep holding their PTYs;
that is where the 73 zombies on this machine come from, and the oldest wrappers are two days old.

Not forced. The launcher's own contract says an ambiguous cleanup stage is the coordinator's
manual call and that residual resources must not be deleted, so the directories and wrappers were
left intact. `orca orchestration worker-release` *did* succeed for all six dispatches, so Orca's
own accounting is settled; only the launcher-side terminal close is outstanding.

Whoever picks this up: decide once whether to (a) teach cleanup to accept a current-generation
settlement proof, (b) run cleanup for a lane *before* rebinding the Run, or (c) accept the wrapper
leak and reap it out of band. Option (b) costs nothing and is available today — clean up last
tick's lanes **before** calling `run-use`.

---

# Continuation — 2026-09-22T16:0xZ (same tick, later messages)

## PR #1825 is MERGED

`f6ef75126 fix(catalog): count the egress ceiling over redis tcp (#1825)` is on `origin/main`.
Verified against the actual `origin/main` tip, not against the merge command — `gh pr merge`
printed nothing at all on success.

Evidence it merged on: Opus seat `ctx_b36c63551f2d` returned **DOES NOT BLOCK, 0 must-fix**,
with 9 mutants applied / 9 killed by named assertions / 0 survivors, a 27-case probe proving
the deleted `refuseErrors` loop is genuinely redundant (negative-controlled 27/27 → 11/27), and
both a plain and a type-aware lint negative control fired. The verdict was **read out of
`review.md` §Verdict and §Must-fix**, not out of the `worker_done` prose. Issue #1824
auto-closed.

Four judgement findings, none landed. Two were filed instead:
- **#1833** — `upstream-ceiling.ts:78-80` is a bare catch, so no `CeilingStoreError` message
  ever reaches an operator. The four diagnostic strings #1825 just added are, today,
  unreadable by anyone. Pre-existing. Includes an AC that no secret may reach the log — that
  store's connection string carries a password.
- **#1834** — `assert.rejects(p, Error, "…")` does not pin which guard fired (the third arg is
  a failure message, not a matcher); a duplicated builder that **dropped the PASSWORD gitleaks
  rationale comment**; and four overclaiming comments.

## `gh pr merge --delete-branch` closed the stacked child PR

`--delete-branch` removed `lifeodyssey/orca-1824-lane`. **PR #1828 was based on that branch**,
and GitHub auto-closed it the moment the base disappeared (`closedAt 16:02:05Z`, seconds after
the merge).

Worse, it could not simply be reopened: `gh pr reopen` fails with *"Could not open the pull
request"* and `gh pr edit --base main` fails with *"Cannot change the base branch of a closed
pull request."* The recovery, in this order:

1. Recreate the deleted ref at the merged tip via the API — no local push, so no pre-push gate:
   `gh api repos/lifeodyssey/animichi/git/refs -f ref=refs/heads/lifeodyssey/orca-1824-lane -f sha=495a12db3…`
2. `gh pr reopen 1828`
3. `gh pr edit 1828 --base main`
4. Delete the temporary ref again — safe now, because #1828 no longer points at it.

#1828 is **OPEN, base `main`**, verified after the ref was deleted again.

**The rule this buys:** in a stack, retarget the children to `main` *before* merging the parent,
or merge the parent **without** `--delete-branch` and clean the branch up afterwards. See
`feedback_stack_prs_and_story_grouping` — it says to point a child's base at the parent branch,
but does not warn that deleting that branch closes the child.

## #1828 is now unblocked but not dispatched

It is `DIRTY` against `main` — a real rebase is needed. The conflict is known from an earlier
trial: `test/repo-config/anitabi-egress-disclosure.test.rb`, **comment-only**, two competing
scan-rationale paragraphs, and the trial resolution that worked was to keep both. Resolving it
means editing a file, which the coordinator role does not do — it needs a writer lane.

Not dispatched this tick because #1557 is in its **paid** 33-case run and #1828's gate is a
catalog lane. Next tick.

## #1557 spend checkpoint: approved, 33-case run authorised

Smoke: 3 cases, 0 failures, `execution_pass=true` and `pi.usage.status=measured` on all three,
`opencode-go:mimo-v2.5` (the corrected binding, **not** xiaomi), commit `ea811590b`, repeat 1,
sampling iid, uploader unconfigured and reported. **Actual spend $0.00309 for 3 cases**;
projection $0.034 linear, $0.063 worst case — 1.86×, inside the ~2× stop band. Catalog is the
authorised local fixture: `wrangler dev` on 127.0.0.1:8791 against test-postgres
`eval1557_catalog_fixture_0949f7237aad`, 5 bangumi / 7 points / 6 aliases / 20 locations /
30 location aliases, all from committed fixtures.

**The near-miss in that checkpoint is the part worth keeping.** The *first* smoke had all three
`web_search` calls abort at the tool's own 10s timeout, because Node's fetch ignores
`HTTP_PROXY`/`HTTPS_PROXY` unless `NODE_USE_ENV_PROXY=1` and this machine runs a Clash proxy on
127.0.0.1:7897. The worker caught it and re-ran. Three things were required back:

1. `NODE_USE_ENV_PROXY=1` goes into the run's recorded **environment identity**, beside model
   and commit. A later run without it is not the same environment.
2. State explicitly whether any case reported `execution_pass=true` **while its only external
   tool silently aborted**. If yes, that is a vacuous pass of the #1439/#1440 shape and the
   harness has a defect independent of this card — measure it, do not fix it there.
3. Keep **both** smoke reports. The rejected one is the evidence for (2).

Also relayed: their message rendered the spend as `/bin/bash.0030929528` — `$0` was expanded by
their shell. Worth knowing when reading any spend figure off this channel.

## Still outstanding at this point

- `orca-1452-lane` push gate still running (now on `--filter anitabi-egress --filter web`);
  PR not yet opened.
- `ctx_83358bdf1323` #1524 round-1 review still running, no `review.md` yet.
- PR #1818 unchanged: still unpushed, `origin` still at the pre-fix `32083575c`, red gate.

## PR #1835 opened for #1452

`orca-1452-lane` pushed (`1d7aaf285`, LOCAL == REMOTE verified) and **PR #1835** opened against
`main`. Verdict read out of `review.md` §VERDICT: *"No. This delta does not block a push."*,
0 must-fix on both axes. Four judgement findings named in the PR body as deliberately deferred;
P1 (a `packages/contract/.eslintignore` containing `test/` still narrows 68 → 34 with the guard
green) and P2 (a substring assert where CI runs `lint`) are the two worth folding in if the
owner prefers that to a card.

**Its gate ran `--filter anitabi-egress --filter web` and passed.** That is a real data point
for PR #1818: `apps/web`'s `chat-page-clarify-pick.test.tsx` passes on a lane that is *not* the
CSP change. It does **not** settle whether #1818's failure is a load flake or caused by the CSP
delta — it only rules out "that test is broken on main." Still worth having before the #1818
lane is dispatched.

## #1524 round-1 review: APPROVE WITH REQUIRED WORDING FIXES

Opus seat `ctx_83358bdf1323`. The mechanism is clean — wrangler 4.132.0's `getSubdomainValues`
quote confirmed verbatim against the installed source (plus a `routes ?? []` normalisation the
writer left implicit), `sealedConfig`/`publish-services.sh` traced to prove the pin reaches the
real deploy surface, **12 mutations / 12 named reds / 0 survivors** (the 6 committed plus 6 the
seat wrote for assertions the writer never probed), the edge TS test proved strictly stronger
via a 2×2 old/new matrix, and the `pr-verification.yml` edit forced red-then-green empirically.
Scope clean: staging untouched, no suppression, no secrets, no deploy.

**Two must-fix, both wording, both about a claim exceeding its evidence:**

**F1 — the control does not discriminate.** The candidate asserted in five places that
production's workers.dev was closed *by the platform's per-Worker subdomain flag*. The seat
measured both halves rather than inheriting the claim: `catalog-staging` and `users-staging`
(deployed, `workers_dev = false`) and a fabricated never-registered host **all** answer an
identical 404 / 17 bytes / `error code: 1042`. Deployed + `workers_dev = true` yields a *real*
response instead (`migrator-staging` a 13-byte Worker-authored 404, `animichi-staging` a 302 to
Access). So a 1042 says the host is closed and **nothing about why**, and AC3's "corrected to
the measured truth" is unmet. The seat also declined to assert the opposite — non-deployment is
the parsimonious reading, but it audited only part of the CD history and would not claim more
than it saw. *This is the exact question the brief asked it to check, and it is the same
finding the previous coordinator's own "measured with a control" summary had already smoothed
over.*

**F2 — "every production surface" is false.** `workers/migrator` `[env.production]` sets
`workers_dev = true` deliberately and documented, and `workers/users` declares no `preview_urls`
at any level. Fix is wording in this PR, not scope expansion.

## Cards filed from the two reviews

| Card | From | What |
|---|---|---|
| **#1833** | #1825 J1 | `upstream-ceiling.ts:78-80` bare catch — no `CeilingStoreError` ever reaches an operator, so the four diagnostics #1825 added are unreadable. Carries an AC that no secret reaches the log; that connection string has a password. |
| **#1834** | #1825 J2/J3/J4 | `assert.rejects(p, Error, …)` pins no guard; a duplicated builder that dropped the PASSWORD gitleaks rationale; four overclaiming comments. |
| **#1836** | #1524 F2 | `workers/users` `preview_urls` unset ⇒ Cloudflare's enabled-by-default per-version preview URLs **with live user-data bindings** on first production deploy. Latent, not live. Also folds users + migrator into the contract with migrator's `true` asserted as intentional. |

## Dispatched: `ctx_ccdff0a16078` — F1/F2 wording fix

Writer `opencode-go/deepseek-v4.1-flash` max (≠ the original `glm-5.3-flash` writer, ≠ the Opus
seat). Wording only, no behaviour change, no file-scope expansion; amends the unpushed commit's
subject to the honest scope, then opens the PR.

## Open question for the owner — the Quality Ratchet on #1524

`ac_total = 2` typed ACs, `ac_with_test = 1`. The `api` AC measures live production
non-exposure — and **F1 establishes that measurement cannot distinguish cause**, so no in-diff
test can carry it. By the letter of `ac_total == ac_with_test` this fails.

The seat reported the number rather than exempting itself, and I am doing the same: the fix lane
is instructed to state it plainly in the PR body as an open question, not to argue it away and
not to invent a test that makes the number look right. **Someone has to decide whether the api
AC should be rewritten** (the config pin is what is actually verifiable) or whether the ratchet
takes an explicit, recorded exemption here. I am not taking that call on a scheduled tick.
