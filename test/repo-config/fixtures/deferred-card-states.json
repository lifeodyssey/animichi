{
  "refreshed": "2026-09-23",
  "command": "for n in $(ruby -rjson -e 'puts JSON.parse(File.read(ARGV[0]))[\"cards\"].map { |c| c[\"number\"] }' test/repo-config/fixtures/deferred-card-states.json); do gh issue view \"$n\" --repo lifeodyssey/animichi --json number,state,stateReason,closedAt,title; done",
  "note": "Read by open-card-deferrals.test.rb, which cannot ask GitHub anything: this file is the repository's committed claim about the cards its live documents still park work on. The command above refreshes the states of the cards already listed; the contract names any card it does not list, so a new deferral tells you which number to add.",
  "cards": [
    {
      "number": 1367,
      "state": "OPEN",
      "title": "ops(delivery): move every job onto oidc and esc, zero github secrets"
    },
    {
      "number": 1370,
      "state": "OPEN",
      "title": "feat(edge): move edge runtime secrets into secrets store"
    },
    {
      "number": 1523,
      "state": "OPEN",
      "title": "ops(infra): renew the staging access service token before 2027-09-08"
    },
    {
      "number": 1593,
      "state": "OPEN",
      "title": "arch(repo): adjudicate where platform semantics fall short"
    },
    {
      "number": 1619,
      "state": "OPEN",
      "title": "feat(delivery): route workers by quota and hand off"
    }
  ]
}
