# The twin-migration rule (docs/ops/neon-test-infra.md) REQUIRES every
# supabase migration to ship with a near-identical auth-stripped atlas twin —
# copy-paste detection across migration trees is structural, not accidental.
sonar.cpd.exclusions=supabase/migrations/**,db/migrations/**
